<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:SQLiteViewer.Converters">

    <FontFamily x:Key="iconfont">
        pack://application:,,,/SQLiteViewer;component/Assets/Fonts/#iconfont
    </FontFamily>

    <!-- 直接使用颜色值，避免资源引用问题 -->
    <SolidColorBrush x:Key="WindowBackground" Color="#FFF5F7FA"/>
    <SolidColorBrush x:Key="PrimaryBlue" Color="#0078D7"/>
    <SolidColorBrush x:Key="LightBlue" Color="#E6F2FA"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#262626"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#595959"/>

    <!-- 科技感按钮样式 -->
    <ControlTemplate TargetType="Button" x:Key="ButtonTemplate">
        <Border Name="back" 
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="4">
            <ContentControl Content="{TemplateBinding Content}" 
                          FontFamily="{StaticResource iconfont}"  
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center"
                          Foreground="{StaticResource TextPrimary}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightBlue}" TargetName="back"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}" TargetName="back"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D0E8FF" TargetName="back"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- 标题栏样式 -->
    <Style x:Key="TitleBarStyle" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#F0F8FF" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
    </Style>

    <!-- 科技感边框样式 -->
    <Style x:Key="TechBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E8F4FF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="#40A9FF" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>